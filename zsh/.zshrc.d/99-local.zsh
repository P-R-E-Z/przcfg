# ~/.zshrc.d/99-local.zsh
# ─── Machine-Specific Overrides ─────────────────────────────────────────────

# Override Starship config (example)
# export STARSHIP_CONFIG="$HOME/.config/starship_fedora.toml"

# Enable extra dev tools on one machine only
# export DEV_MODE=1

# Secret tokens (for local dev only, never commit this)
# export TEST_API_KEY="s3cret-dev-token"

# Example: different PYENV_ROOT on shared workstation
# export PYENV_ROOT="$HOME/.pyenv-alt"
